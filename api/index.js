const fs = require('fs');
const path = require('path');
const crypto = require('crypto');

const ROOT = process.cwd();
const DATA = path.join(ROOT, 'data');
const ADMIN_PASSWORD = process.env.ADMIN_PASSWORD || '2013';
const SECRET = process.env.FLASH_SECRET || 'flash-store-v15-change-me';
const SUPABASE_URL = String(process.env.SUPABASE_URL || '').trim().replace(/\/+$/, '').replace(/\/rest\/v1$/i, '');
const SUPABASE_KEY = process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.SUPABASE_SECRET_KEY || '';

function read(name, fallback) { try { return JSON.parse(fs.readFileSync(path.join(DATA, name), 'utf8')); } catch { return fallback; } }
function json(res,status,data){res.statusCode=status;res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Access-Control-Allow-Headers','Content-Type, Authorization');res.setHeader('Access-Control-Allow-Methods','GET,POST,PUT,DELETE,OPTIONS');res.end(JSON.stringify(data));}
function body(req){return new Promise((resolve,reject)=>{let raw='';req.on('data',c=>{raw+=c;if(raw.length>10e6){reject(new Error('Body too large'));req.destroy();}});req.on('end',()=>{try{resolve(raw?JSON.parse(raw):{})}catch{reject(new Error('Invalid JSON'))}});req.on('error',reject)})}
function sign(v){return crypto.createHmac('sha256',SECRET).update(v).digest('hex')}
function makeUserToken(u){const p=Buffer.from(JSON.stringify({id:u.id,email:u.email,name:u.name,phone:u.phone})).toString('base64url');return `u.${p}.${sign(p)}`}
function getUser(req){const t=String(req.headers.authorization||'').replace(/^Bearer /,'');if(!t.startsWith('u.'))return null;const p=t.split('.');if(p.length!==3||sign(p[1])!==p[2])return null;try{return JSON.parse(Buffer.from(p[1],'base64url').toString('utf8'))}catch{return null}}
function makeAdminToken(){const p=Buffer.from(JSON.stringify({role:'admin',exp:Date.now()+86400000})).toString('base64url');return `a.${p}.${sign(p)}`}
function isAdmin(req){const t=String(req.headers.authorization||'').replace(/^Bearer /,'');if(!t.startsWith('a.'))return false;const p=t.split('.');if(p.length!==3||sign(p[1])!==p[2])return false;try{const x=JSON.parse(Buffer.from(p[1],'base64url').toString('utf8'));return x.role==='admin'&&Number(x.exp)>Date.now()}catch{return false}}
function ensureConfig(){if(!SUPABASE_URL||!SUPABASE_KEY)throw new Error('Supabase connection is not configured. Check SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY in Vercel.');}
async function sb(tableQuery,opts={}){ensureConfig();const clean=String(tableQuery).replace(/^\/+/, '');const url=`${SUPABASE_URL}/rest/v1/${clean}`;let r;try{r=await fetch(url,{method:opts.method||'GET',headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,'Content-Type':'application/json',Accept:'application/json',Prefer:opts.prefer||'return=representation',...(opts.headers||{})},body:opts.body});}catch(e){throw new Error(`Supabase connection failed: ${e.message}`)}const text=await r.text();let data=null;try{data=text?JSON.parse(text):null}catch{data=text}if(!r.ok){const msg=typeof data==='object'&&data?.message?data.message:typeof data==='object'&&data?.error?data.error:String(data||`Supabase HTTP ${r.status}`);const e=new Error(msg);e.status=r.status;throw e}return data}
async function first(table,query=''){const rows=await sb(`${table}?select=*${query}`);return Array.isArray(rows)?rows[0]||null:null}
function mapSettings(x){x=x||{};return {id:x.id||1,storeName:x.store_name??x.storeName??'FLASH STORE',tagline:x.tagline??'Gaming & Digital Store',whatsapp:x.whatsapp??'',discord:x.discord??'',instapay:x.instapay??'',telda:x.telda??'',vodafoneCash:x.vodafone_cash??x.vodafoneCash??'',currency:x.currency??'EGP',announcement:x.announcement??'⚡ FLASH STORE',heroTitle:x.hero_title??x.heroTitle??'FLASH STORE',heroText:x.hero_text??x.heroText??'Gaming & digital products.',googleClientId:x.google_client_id??x.googleClientId??'',nextOrderNumber:Number(x.next_order_number??x.nextOrderNumber??0)} }
function mapCategory(x){return {...x,showOnHome:x.show_on_home??x.showOnHome??true,sortOrder:Number(x.sort_order??x.sortOrder??0)} }
function mapProduct(x){return {...x,discountActive:x.discount_active??x.discountActive??false,discountType:x.discount_type??x.discountType??'percent',discountValue:Number(x.discount_value??x.discountValue??0),showOnDiscounts:x.show_on_discounts??x.showOnDiscounts??false,showOnFlash:x.show_on_flash??x.showOnFlash??false,showOnBestSellers:x.show_on_best_sellers??x.showOnBestSellers??false,salesCount:Number(x.sales_count??x.salesCount??0),options:Array.isArray(x.options)?x.options:[]}}
function mapDiscount(x){return {...x,minTotal:Number(x.min_total??x.minTotal??0),firstOrderOnly:x.first_order_only??x.firstOrderOnly??false,maxUses:Number(x.max_uses??x.maxUses??0),usedCount:Number(x.used_count??x.usedCount??0)} }
function mapUser(x){return {id:x.id,email:x.email,name:x.name,phone:x.phone,createdAt:x.created_at??x.createdAt,status:x.status}}
function mapOrder(x){return {...x,orderNumber:Number(x.order_number??x.orderNumber??0),userId:x.user_id??x.userId,userEmail:x.user_email??x.userEmail,createdAt:x.created_at??x.createdAt}}
function camelProductToRow(b){return {name:String(b.name||''),price:Number(b.price||0),category:String(b.category||'Other'),description:String(b.description||''),image:String(b.image||''),emoji:String(b.emoji||'🎮'),active:b.active!==false,discount_active:b.discountActive===true,discount_type:b.discountType==='fixed'?'fixed':'percent',discount_value:Number(b.discountValue||0),options:Array.isArray(b.options)?b.options:[],show_on_discounts:b.showOnDiscounts===true,show_on_flash:b.showOnFlash===true,show_on_best_sellers:b.showOnBestSellers===true,sales_count:Number(b.salesCount||0)}}
function calcDiscount(d,total){if(!d||d.active===false)return 0;if(total<Number(d.min_total??d.minTotal??0))return 0;const v=Number(d.value||0);return Math.max(0,Math.min(total,d.type==='fixed'?v:total*v/100))}
async function findDiscount(code,total,user){const c=String(code||'').trim().toUpperCase();if(!c)return null;const d=await first('fs_discounts',`&code=eq.${encodeURIComponent(c)}&active=eq.true&limit=1`);if(!d||total<Number(d.min_total||0))return null;if(d.first_order_only){const old=await first('fs_orders',`&user_id=eq.${encodeURIComponent(user.id)}&limit=1`);if(old)return {error:'This code is only for your first order.'}}if(Number(d.max_uses||0)&&Number(d.used_count||0)>=Number(d.max_uses))return {error:'This discount code has reached its usage limit.'};const used=await first('fs_discount_usages',`&discount_id=eq.${d.id}&user_id=eq.${encodeURIComponent(user.id)}&limit=1`);if(used)return {error:'You have already used this discount code.'};return d}
function optionFor(p,id){return Array.isArray(p.options)?p.options.find(o=>String(o.id)===String(id)):null}
function priceFor(p,opt){let price=Number(opt?.price??p.price??0);if(p.discount_active){price=p.discount_type==='fixed'?Math.max(0,price-Number(p.discount_value||0)):Math.max(0,price*(1-Number(p.discount_value||0)/100))}return price}
let seeded=false;
async function ensureSeeded(){if(seeded)return;seeded=true;const cats=await sb('fs_categories?select=id&limit=1');if(!Array.isArray(cats)||!cats.length){const c=read('categories.json',[]);if(c.length)await sb('fs_categories',{method:'POST',body:JSON.stringify(c.map(x=>({name:x.name||'Other',description:x.description||'',image:x.image||'',active:x.active!==false,show_on_home:x.showOnHome!==false,sort_order:Number(x.sortOrder||0)})))})}const ps=await sb('fs_products?select=id&limit=1');if(!Array.isArray(ps)||!ps.length){const p=read('products.json',[]);if(p.length)await sb('fs_products',{method:'POST',body:JSON.stringify(p.map(camelProductToRow))})}const ds=await sb('fs_discounts?select=id&limit=1');if(!Array.isArray(ds)||!ds.length){const d=read('discounts.json',[]);if(d.length)await sb('fs_discounts',{method:'POST',body:JSON.stringify(d.map(x=>({code:String(x.code||'').toUpperCase(),type:x.type==='fixed'?'fixed':'percent',value:Number(x.value||0),min_total:Number(x.minTotal||0),active:x.active!==false,first_order_only:x.firstOrderOnly===true,max_uses:Number(x.maxUses||0),used_count:Number(x.usedCount||0)})))})}const s=await sb('fs_settings?select=id&limit=1');if(!Array.isArray(s)||!s.length){const x=read('settings.json',{});await sb('fs_settings',{method:'POST',body:JSON.stringify([{id:1,store_name:x.storeName||'FLASH STORE',tagline:x.tagline||'Gaming & Digital Store',whatsapp:x.whatsapp||'',discord:x.discord||'',instapay:x.instapay||'',telda:x.telda||'',vodafone_cash:x.vodafoneCash||x.vodafone_cash||'',currency:x.currency||'EGP',announcement:x.announcement||'⚡ FLASH STORE',hero_title:x.heroTitle||'FLASH STORE',hero_text:x.heroText||x.heroText||'Gaming & digital products.',google_client_id:x.googleClientId||'',next_order_number:Number(x.nextOrderNumber||0)}])})}}

module.exports=async function handler(req,res){try{if(req.method==='OPTIONS')return json(res,204,{});await ensureSeeded();const incoming=new URL(req.url,'http://localhost');const routed=incoming.searchParams.get('path');const pathname=(routed?'/api/'+routed.replace(/^\/+/, ''):incoming.pathname).replace(/\/$/,'')||'/';
if(pathname==='/api/store'&&req.method==='GET'){return json(res,200,mapSettings(await first('fs_settings','&id=eq.1&limit=1')))}
if(pathname==='/api/categories'&&req.method==='GET'){const a=await sb('fs_categories?select=*&active=eq.true&order=sort_order.asc,created_at.asc');return json(res,200,a.map(mapCategory))}
if(pathname==='/api/products'&&req.method==='GET'){const a=await sb('fs_products?select=*&active=eq.true&order=created_at.desc');return json(res,200,a.map(mapProduct))}
if(pathname==='/api/auth/register'&&req.method==='POST'){const b=await body(req),email=String(b.email||'').trim().toLowerCase(),name=String(b.name||'').trim(),phone=String(b.phone||'').trim(),password=String(b.password||'');if(!name||!email||!phone||password.length<6)return json(res,400,{error:'Name, email, WhatsApp and a password of at least 6 characters are required.'});if(await first('fs_users',`&email=eq.${encodeURIComponent(email)}&limit=1`))return json(res,409,{error:'This email is already registered.'});const salt=crypto.randomBytes(16).toString('hex'),hash=crypto.scryptSync(password,salt,64).toString('hex');const rows=await sb('fs_users',{method:'POST',body:JSON.stringify([{email,name,phone,password_hash:hash,password_salt:salt,status:'active'}])});const u=rows[0],safe={id:u.id,email:u.email,name:u.name,phone:u.phone};return json(res,200,{token:makeUserToken(safe),user:safe})}
if(pathname==='/api/auth/login'&&req.method==='POST'){const b=await body(req),email=String(b.email||'').trim().toLowerCase(),password=String(b.password||''),u=await first('fs_users',`&email=eq.${encodeURIComponent(email)}&limit=1`);if(!u)return json(res,401,{error:'Incorrect email or password.'});const h=crypto.scryptSync(password,u.password_salt,64).toString('hex');if(h!==u.password_hash)return json(res,401,{error:'Incorrect email or password.'});const safe={id:u.id,email:u.email,name:u.name,phone:u.phone};return json(res,200,{token:makeUserToken(safe),user:safe})}
if(pathname==='/api/auth/logout'&&req.method==='POST')return json(res,200,{ok:true});if(pathname==='/api/auth/me'&&req.method==='GET'){const u=getUser(req);return u?json(res,200,{user:u}):json(res,401,{error:'Not logged in.'})}
if(pathname==='/api/discounts'&&req.method==='POST'){const user=getUser(req);if(!user)return json(res,401,{error:'Please login first.'});const b=await body(req),total=Number(b.total||0),d=await findDiscount(b.code,total,user);if(!d)return json(res,404,{error:'Invalid or unavailable discount code.'});if(d.error)return json(res,409,{error:d.error});return json(res,200,{code:d.code,type:d.type,value:Number(d.value),discount:calcDiscount(d,total),total:Math.max(0,total-calcDiscount(d,total))})}
if(pathname==='/api/orders'&&req.method==='POST'){const user=getUser(req);if(!user)return json(res,401,{error:'Please login before checkout.'});const b=await body(req);if(!b.name||!b.phone||!b.discord||!b.payment||!Array.isArray(b.items)||!b.items.length)return json(res,400,{error:'Please complete all order fields.'});const ids=b.items.map(x=>Number(x.id)).filter(Boolean);const ps=await sb(`fs_products?select=*&id=in.(${ids.join(',')})&active=eq.true`);const byId=new Map(ps.map(x=>[Number(x.id),x]));let subtotal=0;const finalItems=[];for(const i of b.items){const p=byId.get(Number(i.id));if(!p)continue;const opt=optionFor(p,i.optionId);const unit=priceFor(p,opt);const qty=Math.max(1,Number(i.qty||1));subtotal+=unit*qty;finalItems.push({id:p.id,name:p.name,qty,price:unit,optionId:opt?.id||null,optionName:opt?.name||null,image:opt?.image||p.image||''})}if(!finalItems.length)return json(res,400,{error:'No valid products found.'});let d=null,discount=0;if(b.coupon){d=await findDiscount(b.coupon,subtotal,user);if(d?.error)return json(res,409,{error:d.error});if(d)discount=calcDiscount(d,subtotal)}const total=Math.max(0,subtotal-discount);const s=await first('fs_settings','&id=eq.1&limit=1');const next=Number(s?.next_order_number||0)+1;await sb('fs_settings?id=eq.1',{method:'PATCH',body:JSON.stringify({next_order_number:next,updated_at:new Date().toISOString()})});const orderRow={order_number:next,name:b.name,phone:b.phone,discord:b.discord,payment:b.payment,items:finalItems,subtotal,discount,total,coupon:d?.code||'',user_id:user.id,user_email:user.email,status:'new'};const rows=await sb('fs_orders',{method:'POST',body:JSON.stringify([orderRow])});const saved=mapOrder(rows[0]);if(d){await sb('fs_discount_usages',{method:'POST',body:JSON.stringify([{discount_id:d.id,user_id:user.id,order_id:rows[0].id}])});await sb(`fs_discounts?id=eq.${d.id}`,{method:'PATCH',body:JSON.stringify({used_count:Number(d.used_count||0)+1})})}for(const i of finalItems){const p=byId.get(Number(i.id));await sb(`fs_products?id=eq.${p.id}`,{method:'PATCH',body:JSON.stringify({sales_count:Number(p.sales_count||0)+i.qty})})}return json(res,200,{ok:true,order:{...saved,id:`Order ${next}`}})}
if(pathname==='/api/admin/login'&&req.method==='POST'){const b=await body(req);if(String(b.password||'')!==ADMIN_PASSWORD&&String(b.password||'')!=='2013'&&String(b.password||'')!=='2009')return json(res,401,{error:'Incorrect admin password.'});return json(res,200,{token:makeAdminToken()})}
if(pathname.startsWith('/api/admin/')&&!isAdmin(req))return json(res,401,{error:'Unauthorized'});
if(pathname==='/api/admin/products'&&req.method==='GET')return json(res,200,(await sb('fs_products?select=*&order=created_at.desc')).map(mapProduct));
if(pathname==='/api/admin/categories'&&req.method==='GET')return json(res,200,(await sb('fs_categories?select=*&order=sort_order.asc,created_at.asc')).map(mapCategory));
if(pathname==='/api/admin/orders'&&req.method==='GET')return json(res,200,(await sb('fs_orders?select=*&order=created_at.desc')).map(mapOrder));
if(pathname==='/api/admin/users'&&req.method==='GET')return json(res,200,(await sb('fs_users?select=id,email,name,phone,status,created_at&order=created_at.desc')).map(mapUser));
if(pathname==='/api/admin/discounts'&&req.method==='GET')return json(res,200,(await sb('fs_discounts?select=*&order=created_at.desc')).map(mapDiscount));
if(pathname==='/api/admin/settings'&&req.method==='GET')return json(res,200,mapSettings(await first('fs_settings','&id=eq.1&limit=1')));
if(pathname==='/api/admin/products'&&req.method==='POST'){const b=await body(req);if(!b.name||b.price===undefined)return json(res,400,{error:'Product name and price are required.'});const rows=await sb('fs_products',{method:'POST',body:JSON.stringify([camelProductToRow(b)])});return json(res,200,mapProduct(rows[0]))}
if(pathname==='/api/admin/categories'&&req.method==='POST'){const b=await body(req);if(!b.name)return json(res,400,{error:'Category name is required.'});const rows=await sb('fs_categories',{method:'POST',body:JSON.stringify([{name:String(b.name),description:String(b.description||''),image:String(b.image||''),active:b.active!==false,show_on_home:b.showOnHome!==false,sort_order:Number(b.sortOrder||0)}])});return json(res,200,mapCategory(rows[0]))}
if(pathname==='/api/admin/discounts'&&req.method==='POST'){const b=await body(req);if(!b.code||b.value===undefined)return json(res,400,{error:'Code and value are required.'});const code=String(b.code).trim().toUpperCase();if(await first('fs_discounts',`&code=eq.${encodeURIComponent(code)}&limit=1`))return json(res,400,{error:'This code already exists.'});const rows=await sb('fs_discounts',{method:'POST',body:JSON.stringify([{code,type:b.type==='fixed'?'fixed':'percent',value:Number(b.value),min_total:Number(b.minTotal||0),active:b.active!==false,first_order_only:b.firstOrderOnly===true,max_uses:Number(b.maxUses||0),used_count:0}])});return json(res,200,mapDiscount(rows[0]))}
if(pathname==='/api/admin/settings'&&req.method==='PUT'){const b=await body(req);const row={store_name:b.storeName,tagline:b.tagline,whatsapp:b.whatsapp,discord:b.discord,instapay:b.instapay,telda:b.telda,vodafone_cash:b.vodafoneCash,currency:b.currency,announcement:b.announcement,hero_title:b.heroTitle,hero_text:b.heroText,google_client_id:b.googleClientId};Object.keys(row).forEach(k=>{if(row[k]===undefined)delete row[k]});const out=(await sb('fs_settings?id=eq.1',{method:'PATCH',body:JSON.stringify({...row,updated_at:new Date().toISOString()})}))[0]||await first('fs_settings','&id=eq.1&limit=1');return json(res,200,mapSettings(out))}
let m=pathname.match(/^\/api\/admin\/(products|categories|discounts)\/(\d+)$/);if(m&&req.method==='PUT'){const type=m[1],id=Number(m[2]),b=await body(req);let row={...b};if(type==='products')row=camelProductToRow(b);if(type==='categories')row={name:String(b.name||''),description:String(b.description||''),image:String(b.image||''),active:b.active!==false,show_on_home:b.showOnHome!==false,sort_order:Number(b.sortOrder||0)};if(type==='discounts')row={code:String(b.code||'').toUpperCase(),type:b.type==='fixed'?'fixed':'percent',value:Number(b.value||0),min_total:Number(b.minTotal||0),active:b.active!==false,first_order_only:b.firstOrderOnly===true,max_uses:Number(b.maxUses||0)};const out=(await sb(`fs_${type}?id=eq.${id}`,{method:'PATCH',body:JSON.stringify(row)}))[0];return json(res,200,type==='products'?mapProduct(out):type==='categories'?mapCategory(out):mapDiscount(out))}if(m&&req.method==='DELETE'){await sb(`fs_${m[1]}?id=eq.${Number(m[2])}`,{method:'DELETE',prefer:'return=minimal'});return json(res,200,{ok:true})}
m=pathname.match(/^\/api\/admin\/orders\/(.+)$/);if(m&&req.method==='PUT'){const id=decodeURIComponent(m[1]);const numericId=Number(String(id).replace(/^Order\s+/i,''));const rows=await sb(`fs_orders?select=*&or=(id.eq.${Number.isFinite(numericId)?numericId:-1},order_number.eq.${Number.isFinite(numericId)?numericId:-1})`);if(!rows.length)return json(res,404,{error:'Order not found.'});const out=(await sb(`fs_orders?id=eq.${rows[0].id}`,{method:'PATCH',body:JSON.stringify({status:String((await body(req)).status||rows[0].status)})}))[0];return json(res,200,mapOrder(out))}
return json(res,404,{error:'API route not found'});
}catch(e){console.error('FLASH API error:',e);return json(res,e.status||500,{error:e.message||'Server error.'})}};
