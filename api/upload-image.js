const {json,env,verifyAdminToken,getCookie}=require("./_lib");
async function isAdmin(req){return !!verifyAdminToken(getCookie(req,"flash_admin"));}
function parseMultipart(req){return new Promise((resolve,reject)=>{const ct=req.headers["content-type"]||"";const m=ct.match(/boundary=(?:"([^"]+)"|([^;]+))/i);if(!m)return reject(Error("Expected multipart/form-data"));const boundary=Buffer.from("--"+(m[1]||m[2]));let chunks=[];req.on("data",c=>chunks.push(c));req.on("end",()=>{try{const buf=Buffer.concat(chunks),parts=[];let start=0;while((start=buf.indexOf(boundary,start))!==-1){const next=buf.indexOf(boundary,start+boundary.length);if(next===-1)break;const part=buf.slice(start+boundary.length+2,next-2);start=next;const sep=part.indexOf(Buffer.from("\r\n\r\n"));if(sep<0)continue;const head=part.slice(0,sep).toString();const body=part.slice(sep+4);const nm=head.match(/name="([^"]+)"/i);const fn=head.match(/filename="([^"]*)"/i);parts.push({name:nm?.[1],filename:fn?.[1]||"",contentType:(head.match(/Content-Type:\s*([^\r\n]+)/i)||[])[1]||"application/octet-stream",data:body});}resolve(parts.find(x=>x.name==="file"));}catch(e){reject(e)}});req.on("error",reject)})}
module.exports=async(req,res)=>{if(req.method!=="POST")return json(res,405,{error:"Method not allowed"});if(!await isAdmin(req))return json(res,401,{error:"Admin login required"});try{const file=await parseMultipart(req);if(!file||!file.data?.length)return json(res,400,{error:"Choose an image first"});if(!/^image\/(png|jpe?g|webp|gif|avif)$/i.test(file.contentType))return json(res,400,{error:"Only image files are allowed"});if(file.data.length>8*1024*1024)return json(res,400,{error:"Image must be 8MB or smaller"});const root=env("SUPABASE_URL").replace(/\/rest\/v1\/?$/,'');const key=env("SUPABASE_SERVICE_ROLE_KEY");const bucket="flash-store";const check=await fetch(root+"/storage/v1/bucket/"+bucket,{headers:{apikey:key,Authorization:"Bearer "+key}});if(!check.ok){const cr=await fetch(root+"/storage/v1/bucket",{method:"POST",headers:{apikey:key,Authorization:"Bearer "+key,"Content-Type":"application/json"},body:JSON.stringify({id:bucket,name:bucket,public:true})});if(!cr.ok){const txt=await cr.text();if(!txt.toLowerCase().includes("already"))throw Error("Could not create image storage bucket: "+txt)}}const ext=(file.filename.split(".").pop()||"jpg").toLowerCase().replace(/[^a-z0-9]/g,"")||"jpg";const path=`admin/${Date.now()}-${Math.random().toString(36).slice(2)}.${ext}`;const up=await fetch(root+"/storage/v1/object/"+bucket+"/"+path,{method:"POST",headers:{apikey:key,Authorization:"Bearer "+key,"Content-Type":file.contentType,"x-upsert":"true"},body:file.data});if(!up.ok)throw Error("Image upload failed: "+await up.text());return json(res,200,{url:root+"/storage/v1/object/public/"+bucket+"/"+path})}catch(e){return json(res,500,{error:e.message})}};